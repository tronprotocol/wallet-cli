#!/usr/bin/expect -f
set timeout 60
log_user 1
spawn java -jar $env(JAR)
expect -re "wallet> $"
send -- "gettransactionbyid $env(TXID)\r"
expect -re "wallet> $"
send -- "gettransactioninfobyid $env(TXID)\r"
expect -re "wallet> $"
send -- "exit\r"
expect eof
